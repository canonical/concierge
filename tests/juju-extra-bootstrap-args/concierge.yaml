juju:
  channel: 4.0/stable
  model-defaults:
    test-mode: "true"
  extra-bootstrap-args: --config model-logs-size=10M

providers:
  microk8s:
    enable: true
    bootstrap: true
    channel: 1.31-strict/stable
    addons:
      - hostpath-storage
      - dns
